MODULE if_else;

VAR
    x, y : INTEGER;

    PROCEDURE iff(a, b :INTEGER) : INTEGER;
    BEGIN
      IF ((a + b) < 18) THEN
        RETURN a + b
      ELSE
        RETURN a-b
      END

    END iff
END if_else.
